terraform {
  backend "azurerm" {
    resource_group_name  = "rg-terraform-state-prod"
    storage_account_name = "sttfstateprod001"
    container_name       = "tfstate"
    key                  = "platform/prod.tfstate"
    use_azuread_auth     = true
    # CI authenticates with OIDC/workload identity environment variables.
    # Never place client secrets, SAS tokens, or access keys here or in -backend-config.
  }
}
